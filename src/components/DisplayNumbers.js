export const DisplayNumber = (number) => {
  return <div>{number}</div>;
};
// import { DisplayNumber } from "./DisplayNumbers"

export default DisplayNumber;
// import DisplayNumber from "./DisplayNumber"
